name: sprinty
options:
  bundleIdPrefix: com.ducdo
  deploymentTarget:
    iOS: "17.0"
  xcodeVersion: "26.3"
  createIntermediateGroups: true
  defaultConfig: Debug

settings:
  base:
    SWIFT_VERSION: "6.0"
    SWIFT_STRICT_CONCURRENCY: complete

configs:
  Debug: debug
  Staging: release
  Release: release

configFiles:
  Debug: sprinty/Configuration/Debug.xcconfig
  Staging: sprinty/Configuration/Staging.xcconfig
  Release: sprinty/Configuration/Release.xcconfig

packages:
  GRDB:
    url: https://github.com/groue/GRDB.swift.git
    from: "7.10.0"
  SQLiteVecKit:
    path: Packages/SQLiteVecKit

targets:
  sprinty:
    type: application
    platform: iOS
    sources:
      - path: sprinty
        excludes:
          - "**/*.xcconfig"
    resources:
      - path: sprinty/Resources/Assets.xcassets
      - path: sprinty/Resources/MiniLM.mlpackage
        optional: true
      - path: sprinty/Resources/vocab.txt
        optional: true
    settings:
      base:
        INFOPLIST_KEY_UIApplicationSceneManifest_Generation: YES
        INFOPLIST_KEY_UIApplicationSupportsIndirectInputEvents: YES
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPad: "UIInterfaceOrientationPortrait UIInterfaceOrientationPortraitUpsideDown UIInterfaceOrientationLandscapeLeft UIInterfaceOrientationLandscapeRight"
        INFOPLIST_KEY_UISupportedInterfaceOrientations_iPhone: "UIInterfaceOrientationPortrait"
        PRODUCT_BUNDLE_IDENTIFIER: com.ducdo.sprinty
        GENERATE_INFOPLIST_FILE: YES
        CODE_SIGN_ENTITLEMENTS: sprinty/sprinty.entitlements
        COACH_API_URL: "http://localhost:8080"
    dependencies:
      - package: GRDB
        product: GRDB
      - package: SQLiteVecKit
        product: SQLiteVecKit

  sprintyTests:
    type: bundle.unit-test
    platform: iOS
    sources:
      - path: Tests
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.ducdo.sprinty-tests
        GENERATE_INFOPLIST_FILE: YES
    dependencies:
      - target: sprinty
      - package: GRDB
        product: GRDB
      - package: SQLiteVecKit
        product: SQLiteVecKit

schemes:
  Debug:
    build:
      targets:
        sprinty: all
        sprintyTests: [test]
    run:
      config: Debug
      storeKitConfiguration: Sprinty.storekit
    test:
      config: Debug
      targets:
        - sprintyTests
    profile:
      config: Debug

  Staging:
    build:
      targets:
        sprinty: all
        sprintyTests: [test]
    run:
      config: Staging
    test:
      config: Staging
      targets:
        - sprintyTests
    profile:
      config: Staging

  Release:
    build:
      targets:
        sprinty: all
    run:
      config: Release
    profile:
      config: Release
